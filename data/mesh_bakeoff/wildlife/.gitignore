# NO image binaries in the repo (user rule: nothing that would ever need
# cleansing from history). Everything is reproducible: seed sweeps + habitat
# renders from gen_*.sh (FLUX prompts and seeds encoded there), the picked
# heroes are copies out of heroes-raw/, and gorgonia's scene extraction from
# extract_gorgonia_scene.py (+ ~/Downloads/gargantua.webp).
heroes-raw/
habitats/
heroes/
# review sheet: references heroes-raw/ + out-wildlife/, both untracked
proto_sheet.html
